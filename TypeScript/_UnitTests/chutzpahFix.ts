// chutzpah v2.2.1 isn't including this block of code in files that have classes extending
// other classes. Should be able to remove once bug is fixed.
var __extends = this.__extends || function (d, b) {
    function __() { this.constructor = d; }
    __.prototype = b.prototype;
    d.prototype = new __();
};