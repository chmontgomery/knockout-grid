module go.Virtualization.VirtualMachine {
	export class ToolsVersionStatus {
		constructor (public order: number, public key: string, public cssClass: string, public text: string, public title: string) { }
	}
}